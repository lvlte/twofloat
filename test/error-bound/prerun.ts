/**
 * @file Pre-test
 *
 * Generates inputs and collect outputs for each functions then export the whole
 * as JSON so we can import it and do proper tests in Julia using BigFloat.
 */

import './algorithms';

